export type Coordinates = {
  latitude: number;
  longitude: number;
};

export type Location = {
  id: string;
  name: string;
  address: string;
  latitude: number;
  longitude: number;
  icon: string;
}

export type Locations = Location[];


