export type UserData = {
  name: number;
  email: string;
  avatarUrl: string;
  token: string;
};

export type AuthData = {
  email: string;
  password: string;
};

export type RegistrationData = {
  name: string;
  email: string;
  password: string;
};
